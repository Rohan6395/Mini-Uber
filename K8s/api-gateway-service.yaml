apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 8080          # Cluster IP port
      targetPort: 8080    # Container port
      nodePort: 30000     # Exposed on localhost:30000 (you can choose any 30000-32767)
