apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: cab-system
data:
  POSTGRES_HOST: postgres
  POSTGRES_USER: admin
  POSTGRES_DB: appdb
  POSTGRES_PORT: "5432"
  RABBITMQ_HOST: rabbitmq
  RABBITMQ_PORT: "5672"
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  NODE_ENV: production
